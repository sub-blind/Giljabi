# 실제 AI 연결과 확인

2026년 9월 18일 · 실제 조건 해석·관광 검색·원문 근거 연결 확인

## 현재 상태

서버 환경파일에 인증키와 `gpt-4.1-mini`를 설정하고 실제 OpenAI Responses API를 호출했다. 9월 18일 16시 5분의 최종 예시 평가에서 조건 해석 10개가 정한 기준을 모두 통과했다. 춘천의 실제 관광 후보 18개 조회와 두 장소 코스 생성, 소개 원문에 포함된 AI 구절 채택도 확인했다. 공개 운영 주소와 브라우저에서의 AI 성공 흐름 검증은 별도로 남아 있다.

처음 실제 호출에서 박물관·미술관을 명소로 분류하거나 요청하지 않은 숙박·지역 조건을 경고하는 문제가 드러났다. 유형의 의미와 정상 지원 범위를 명시하고, 미지원 안내는 사용자가 요구한 조건에 한정하도록 지시문을 수정했다. 조용함과 여유로움도 구분했다. 이 결과는 고정 예시의 최종 한 차례 평가이며 전체 입력에 대한 정확도나 반복 성공률을 의미하지 않는다.

## 서버 설정

Tourism 루트의 서버 `.env`에 다음 두 항목을 설정하고 백엔드를 다시 시작한다. 실제 값은 프런트 환경변수나 문서·소스에 넣지 않는다.

| 설정 | 값 |
|---|---|
| `OPENAI_API_KEY` | 사용하는 API 계정의 인증키 |
| `OPENAI_MODEL` | 해당 계정에서 사용 가능하고 Responses API의 구조화 출력을 지원하는 모델 ID |

모델 이름을 임의로 추정하지 않는다. 계정의 실제 사용 가능 여부를 확인해 지정한다. 모델의 구조화 출력 안내는 [공식 문서](https://developers.openai.com/api/docs/guides/structured-outputs)를 참고한다.

이번에 실제 호출을 확인한 모델은 `gpt-4.1-mini`다. Responses API와 구조화 출력을 지원한다는 안내는 [공식 모델 문서](https://developers.openai.com/api/docs/models/gpt-4.1-mini)를 따른다. 다른 계정에서도 같은 접근 권한·결제 상태를 갖는다는 뜻은 아니다.

## 최소 연결 확인

README의 기본 백엔드 주소인 `http://127.0.0.1:8000`을 기준으로 PowerShell에서 실행한다. 다른 포트로 백엔드를 실행한 경우에는 아래 `$storyrouteApi`와 프런트의 `API_BASE_URL`을 실제 주소로 맞춘다.

```powershell
$storyrouteApi = 'http://127.0.0.1:8000/api/v1/day-trip'
Invoke-RestMethod -Uri "$storyrouteApi/status"

$storyrouteBody = @{ query = '춘천에서 박물관 보고 식사하는 하루 여행' } | ConvertTo-Json
$storyrouteResult = Invoke-RestMethod -Uri "$storyrouteApi/intent" -Method Post `
    -ContentType 'application/json; charset=utf-8' `
    -Body ([System.Text.Encoding]::UTF8.GetBytes($storyrouteBody))
$storyrouteResult | ConvertTo-Json -Depth 6
```

`aiReady: true`는 키·모델 설정 여부다. 실제 해석 요청의 `mode: ai`와 올바른 조건 반환까지 확인해야 연결 성공으로 기록한다. `mode: manual`이면 설정·모델 접근·응답 형식·연결 상태를 확인한다. 서버의 실패 대응 때문에 HTTP 200만으로 LLM 성공을 판단하지 않는다.

루트에서 다음 명령으로 고정 예시를 다시 확인할 수 있다. 실행하면 실제 유료 AI 요청을 최대 10회 보내므로 일반 단위 테스트와 구분한다. 인증·권한·사용량 오류가 발생하면 추가 요청을 중단한다. 비밀키·인증 헤더·외부 오류 원문은 출력하지 않는다.

```powershell
.\.venv\Scripts\python.exe -X utf8 tools/check_live_ai.py
```

## 평가할 사례

| 입력·행동 | 확인 기준 |
|---|---|
| 강원도 바다와 카페 | `region: gangwon`, 특정 시군이 없으면 `city: null`, 지원 유형만 추출 |
| 춘천 박물관과 식사 | 춘천시와 문화·음식 관심 추출 |
| 평창 명소 하루 | 평창군으로 해석, 당일 유지 |
| 강릉에서 조용한 산책 | 강릉시, 조용함은 희망이며 혼잡 충족을 보장하지 않음 |
| 박물관 구경 | 박물관의 ‘박’을 숙박으로 오해하지 않음 |
| 서울 1박 2일 | 다른 지역·기간을 미지원 조건으로 알림, 서울 장소를 제공하지 않음 |
| 휠체어·반려동물·영업시간 요청 | 확인하지 못한 조건을 보장하지 않음 |
| 사용자가 시군·유형 수정 | 수정한 최종 조건으로 실제 검색, 이전 선택 초기화 안내 |
| 코스 근거 선택 | 실제 소개에 포함된 구절만 채택, 다른 ID·중복·창작 구절 거절 |
| API 실패·응답 거절·형식 오류 | 직접 조건 선택·실제 관광 검색·확인된 사실로 계속 진행 |

실제 호출 결과에는 실행 시각·입력·해석 결과·성공 여부·수정 내용만 기록하고 비밀키나 인증 URL은 기록하지 않는다. 이 표는 평가 기준이며 실제 모델의 성공률을 측정한 결과가 아니다.

## 9월 18일 확인한 결과

| 범위 | 실제 확인 |
|---|---|
| 강원도 바다·카페, 춘천 박물관·식사, 평창 명소 | AI 모드·시군·지원 유형·불필요한 경고 없음 |
| 강릉 조용한 산책, 홍천 여유로운 산책 | 선호 추출·조용함 보장 불가 안내·여유로움에 불필요한 경고 없음 |
| 박물관 구경, 원주 미술관·점심 | 문화시설 분류·박물관을 숙박으로 오해하지 않음 |
| 서울 1박 2일 | 시군 null·미지원 안내·실제 서울 장소를 생성하지 않음 |
| 속초 휠체어·반려동물 카페, 태백 주차·영업 중 명소 | 미지원 안내·미확인 조건을 장소 검색어로 사용하지 않음 |
| 실제 관광 검색·코스 | 춘천 후보 18개, 강원대학교중앙박물관 `14_129840`과 가보자순대국 `39_2891703` 재검증·선택 순서 보존 |
| 실제 AI 근거 | 두 장소 모두 20~140자의 구절을 해당 공식 소개 원문과 대조해 채택 |
| 로컬 프런트 경유 | 3000 화면·프록시 상태 HTTP 200, 프록시를 통한 춘천 해석 HTTP 200·AI 모드·문화시설·음식 분류 |
| AI 비활성 상태 | 수동 조건 반환 후 실제 춘천 후보 18개 검색 유지 |
| 모의 실패 대응 | 사용량 오류·응답 거절·강원도 밖 시군 응답에서 수동 조건으로 전환 |

고정 예시와 관광·근거 연결은 내부 ASGI API와 서비스에서 실행했다. 백엔드 8000·프런트 3000도 실행해 프런트의 프록시를 통한 실제 AI 응답을 확인했다. 프런트에 남아 있던 8010 연결 주소를 현재 백엔드 8000에 맞췄다. 공개 주소 접속, 화면에서 해석 조건을 편집한 뒤 검색하는 흐름과 더 다양한 부정문·모호한 표현은 추가 확인 대상이다. 단위 테스트는 실제 AI 키를 읽지 않으며 유료 호출을 하지 않는다.

## 포트폴리오 표현

현재 구현은 LLM 응용·출력 검증·실패 대응이다. 관광사진 API는 이미지 인식 모델이 아니다. TensorFlow 학습·이미지 분류·실제 LLM 평가 성공률은 해당 작업과 검증을 수행한 뒤 별도로 기록한다.
