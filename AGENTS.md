# Tourism 프로젝트 작업 기준

## 언어

- README, 기획서, API 설명, 실행 안내, 검증 기록 등 모든 작업 문서는 한국어로 작성한다.
- 화면 안내·오류·완료 메시지도 자연스러운 한국어를 사용한다.
- 코드 식별자, 명령어, 환경변수, API 경로, 라이브러리 이름은 원래 표기를 유지한다.

## 현재 개발 기준

- 작업 대상은 이 Tourism 저장소다.
- 최신 기획 기준은 `docs/PROJECT_PLAN.md`, `docs/MVP_USER_SCENARIO.md`, `docs/API_SELECTION_GANGWON.md`다. `docs/FRONTEND_API_SPEC.md`는 현재 구현 상태를 설명한다.
- `storyroute_omx_dev_ready_pack`은 이전의 넓은 범위를 담은 참고 자료다. 현재 기능 범위는 최신 문서를 우선한다.
- 한 사람이 프런트와 백엔드를 개발한다. 사용자가 정정한 대상은 강원특별자치도 전체다. 당일 여행, 최대 세 장소를 기준으로 하며 한두 장소로도 코스를 만들 수 있다.
- 현재 구현은 강원도 전체 검색과 18개 시군 선택을 지원한다. 국문 관광정보의 지역코드는 실제 목록에서 조회하고 연관 관광지의 별도 코드는 공식 코드표를 사용한다. 서비스별 ID를 직접 결합하지 않는다.
- 기존 Next.js·React·TypeScript와 FastAPI 구조를 사용한다.

## 구현과 검증

- 프런트는 내부 `/api/v1/day-trip` API만 호출한다. 외부 관광·LLM 인증키는 서버 설정에서 관리한다.
- 최종 코스의 장소를 실제 관광 API에서 재검증한다. AI 단서는 소개 원문과 대조한다.
- 관광사진은 촬영지를 확인하고 지역 검색으로 연결한다. 오디오·연관 정보는 상세에서 필요할 때 조회하며 실패해도 기본 장소 조회를 유지한다. 연관 자료의 기준월을 표시한다.
- 무장애 정보는 상세에서 필요할 때 조회한다. 이름·주소·제공 좌표로 동일 장소를 확인하고 제공된 편의 항목만 표시한다. 빈 정보를 이용 불가로 추정하거나 전체 여행 가능 여부를 판정하지 않는다.
- API가 바뀌면 타입, 계약, 문서를 함께 갱신한다.
- 자동차 이동은 서버에서 장소 ID를 재검증한 뒤 카카오모빌리티로 조회한다. 경로·시간은 자동차 기준 조회 시점의 예상값이며 첫 장소까지의 이동·체류·주차 시간은 제외한다. 순서 변경 후 이전 경로를 숨기고 다시 확인하게 한다. 실패한 구간은 직선 경로·가짜 시간으로 대체하지 않는다.
- 방문 체크와 장소 메모는 직접 남기는 브라우저 기록이다. GPS 인증·기기 간 동기화로 표현하지 않는다.
- 모의 응답 검증과 실제 외부 연결 확인 결과를 구분해서 기록한다.
- 완료 전 프런트 빌드·타입·린트와 백엔드 테스트를 확인한다.
